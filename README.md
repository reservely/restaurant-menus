# menu



<!-- let menuSchema = new mongoose.Schema({
  restaurant_id: Number,
  categories: [{
    name: String,
    description: String,
    items: [{
      item_name: String,
      item_description: String,
      item_price: Number,
      item_option: String
    }]
  }]
}); -->